export enum URL_PATHS {
  ROOT = "/",
  PROFILE = "/profile",
  TRANSFERS = "/transfers",
  LOGIN = "/login",
  SIGN_UP = "/signup",
}

export enum LOCAL_STORAGE {
  LOGGED_CLIENT_ID = "logged_client_id",
}
