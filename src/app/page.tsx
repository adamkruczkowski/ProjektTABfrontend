import { Button } from "@nextui-org/button";
import { Input } from "@nextui-org/input";

const Home = () => {
  return (
    <div className="flex flex-col gap-4 items-center justify-center h-full flex-1 my-auto">
      <h3 className="text-4xl font-bold text-center">
        Witaj w banku największych możliwości
      </h3>
      <h4 className="text-2xl">Załóż konto już teraz!</h4>
    </div>
  );
};

export default Home;
